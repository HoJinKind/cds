CREATE TABLE users (
    name VARCHAR(255) NOT NULL,
    salary VARCHAR(255) NOT NULL,
    PRIMARY KEY (name)
);

INSERT INTO users(name, salary) VALUES
('Tim', 1.1),
('Tom', 100.0),
('Tam', 111.0),
('Tang', 122.0);